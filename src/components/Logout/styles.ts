import styled from 'styled-components';

import { Container } from 'atoms/Description/styles';

export const Button = styled(Container)`
  position: absolute;
  cursor: pointer;
  top: 20px;
  right: 20px;
`;
